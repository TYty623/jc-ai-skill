---
name: jc-ai-skill
description: 设计或扩展 Java/Spring Boot AI 应用中的 RAG、受控工具调用、MCP 集成和多 Agent 协作。适用于把业务需求落地为可观测、可验证的 Java AI 服务。
---

# Java AI Agent Engineering

将需求映射到清晰的 AI 应用边界，而不是把模型调用、检索、工具执行和业务事务混在同一个控制器中。

## 适用范围

- Java 或 Spring Boot 服务需要接入对话、RAG、MCP 或 Agent 能力。
- 业务 Agent 需要调用查询、报表、通知等工具，并要限制可执行范围。
- 多 Agent 任务需要任务状态、补充信息和异步结果交接。

不要将此 Skill 用于只需确定性 CRUD 的普通接口；先确认模型推理、检索或工具编排确实带来收益。

## 选择实现边界

1. 普通问答或结构化抽取：把模型适配、提示词和输出校验留在 AI 应用层，控制器只处理 HTTP 协议。
2. 文档问答：建立“导入、解析、切分、索引、检索、重排、带来源回答”的链路；每一步都能独立观测和替换。
3. 工具调用：先定义输入 schema、权限与副作用等级，再向模型暴露最小工具集。写操作、资金、消息发送等操作必须经业务确认。
4. 跨 Agent 协作：用明确的任务 ID、状态和输入缺口表达推进过程；缺参时请求补充信息，不要猜测参数。

## 实施顺序

1. 写清楚用户目标、允许的数据源、可用工具和成功标准。
2. 先实现确定性的业务服务与 DTO，再让 Agent 通过窄接口调用它们。
3. 为检索记录来源、文档版本、命中片段和延迟；为工具记录调用者、参数摘要、结果和失败原因。
4. 为模型输出、检索质量和工具调用分别定义测试集，避免只测试聊天文本是否“看起来合理”。
5. 上线前审查提示词注入、越权工具调用、PII 泄露、重试风暴和长任务恢复策略。

## 复用资料

- 阅读 [架构提炼](references/architecture.md) 以选择模块边界。
- 阅读 [能力映射](references/capability-map.md) 以定位适合复用的模式。
- [ToolInvocationPolicy.java](examples/java/ToolInvocationPolicy.java) 是零依赖的允许集示例；将它接到真实身份认证和审计链路中，不要把客户端声明直接当作权限。

## 边界说明

本仓库是从本地 Java AI 项目提炼的原创工程指南和独立示例，不含原项目源码、配置、密钥或课程材料。实际接入时应遵守模型服务、数据源和业务系统各自的权限约束。
